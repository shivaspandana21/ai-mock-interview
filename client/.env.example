# Backend API URL for production.
# For local dev, you can leave this empty (Vite handles relative /api paths correctly locally)
# or specify the literal path if your backend is on a different port and not proxied.
VITE_API_URL=https://ai-mock-interview-1bty.onrender.com
